{% extends 'base.html.twig' %}

{% block title %}Convention{% endblock %}

{% block body %}
    <h1>Convention</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ convention.id }}</td>
            </tr>
            <tr>
                <th>DateDebut</th>
                <td>{{ convention.dateDebut ? convention.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>DateFin</th>
                <td>{{ convention.dateFin ? convention.dateFin|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ convention.montant }}</td>
            </tr>
            <tr>
                <th>LibellePDF</th>
                <td>{{ convention.libellePDF }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('convention_index') }}">back to list</a>

    <a href="{{ path('convention_edit', {'id': convention.id}) }}">edit</a>

    {{ include('convention/_delete_form.html.twig') }}
{% endblock %}
