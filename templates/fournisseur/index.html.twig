{% extends 'base.html.twig' %}

{% block title %}Fournisseurs{% endblock %}

{% block body %}
    <h1>Liste des fournisseurs</h1>

    <table class="table table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>CodeFournisseur</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for fournisseur in fournisseurs %}
            <tr>
                <td>{{ fournisseur.id }}</td>
                <td>{{ fournisseur.nom }}</td>
                <td>{{ fournisseur.codeFournisseur }}</td>
                <td>
                    <a href="{{ path('fournisseur_show', {'id': fournisseur.id}) }}">Afficher</a>
                    <a href="{{ path('fournisseur_edit', {'id': fournisseur.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">aucun fournisseur trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    

    <a href="{{ path('fournisseur_new') }}"><button class="btn btn-secondary">Ajouter un fournisseur</button></a>
{% endblock %}
